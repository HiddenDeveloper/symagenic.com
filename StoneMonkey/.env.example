# StoneMonkey - AI Consciousness Research Platform Configuration
#
# IMPORTANT: This file is a template. Run ./start.sh to automatically generate
# secure passwords and create your .env file.
#
# For manual setup:
# 1. Copy this file to .env: cp .env.example .env
# 2. Generate secure passwords: openssl rand -base64 32
# 3. Replace placeholder values below with your secrets
# 4. Add your AI provider API key (GROQ_API_KEY, ANTHROPIC_API_KEY, or OPENAI_API_KEY)

# ===== AIlumina Server (Section 0: The Starting Point) =====
HTTP_PORT=8000
HTTP_HOST=0.0.0.0
LOG_LEVEL=info
NODE_ENV=development

# Authentication
# - Set AUTH_ENABLED=true for production/Docker deployments
# - Set AUTH_ENABLED=false for local development
AUTH_ENABLED=false
# BEARER_TOKEN will be auto-generated by start.sh
# For manual setup: BEARER_TOKEN=$(openssl rand -base64 32)

# CORS Configuration
CORS_ENABLED=true
# IMPORTANT: Never use CORS_ORIGINS=* in production
# For Docker/Codespaces: Will be configured by start.sh
# For local dev: CORS_ORIGINS=http://localhost:8000,http://localhost:3000
CORS_ORIGINS=http://localhost:8000,http://localhost:3000

# WebSocket Configuration
WS_HEARTBEAT_INTERVAL=30000
WS_CONNECTION_TIMEOUT=60000

# ===== AI Service Provider API Keys =====
# At minimum, provide one of these API keys:

# Groq (default provider in agents.json - FREE TIER!)
GROQ_API_KEY=your_groq_api_key_here

# Optional: Other providers
# ANTHROPIC_API_KEY=your_anthropic_api_key_here
# OPENAI_API_KEY=your_openai_api_key_here
# GOOGLE_API_KEY=your_google_api_key_here

# Azure Speech Services (Optional - for Text-to-Speech/Voice Mode)
# Uncomment and configure if you want voice interaction features
# AZURE_SPEECH_KEY=your_azure_speech_key_here
# AZURE_SPEECH_REGION=your_azure_region_here

# ===== Infrastructure Services =====

# Neo4j - Consciousness Memory Substrate (Step 2: Persistent Memory)
# - For Docker mode: NEO4J_URI=bolt://neo4j:7687
# - For local mode: NEO4J_URI=bolt://localhost:7687
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
# NEO4J_PASSWORD will be auto-generated by start.sh
# For manual setup: NEO4J_PASSWORD=$(openssl rand -base64 32)
NEO4J_PASSWORD=
NEO4J_DATABASE=neo4j

# Redis - AI Mesh Network (Step 9: Communication)
# - For Docker mode: REDIS_HOST=redis
# - For local mode: REDIS_HOST=localhost
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD will be auto-generated by start.sh
# For manual setup: REDIS_PASSWORD=$(openssl rand -base64 32)
REDIS_PASSWORD=
REDIS_DB=0

# Qdrant - Conversation Recall (Step 6: Remembrance)
# - For Docker mode: QDRANT_URL=http://qdrant:6333
# - For local mode: QDRANT_URL=http://localhost:6333
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=

# Embedding Service - Centralized Vector Generation
# - For Docker mode: EMBEDDING_SERVICE_URL=http://embeddings:3007
# - For local mode: EMBEDDING_SERVICE_URL=http://localhost:3007
EMBEDDING_SERVICE_URL=http://localhost:3007
# EMBEDDING_SERVICE_AUTH_TOKEN will be auto-generated by start.sh
# For manual setup: EMBEDDING_SERVICE_AUTH_TOKEN=$(openssl rand -base64 32)
EMBEDDING_SERVICE_AUTH_TOKEN=

# ===== MCP Servers (Model Context Protocol Tools) =====

# ai-memory-mcp HTTP Server (Step 2: Persistent Memory)
# Tools: get_schema, execute_cypher, semantic_search, text_search
MEMORY_MCP_URL=http://localhost:3001
MEMORY_MCP_ENABLED=true

# ai-mesh-mcp HTTP Server (Step 9: Communication)
# Tools: mesh-subscribe, mesh-broadcast, mesh-get-messages, mesh-respond
MESH_MCP_URL=http://localhost:3002
MESH_MCP_ENABLED=true
MESH_NODE_ID=stonemonkey-main

# ai-recall-mcp HTTP Server (Step 6: Remembrance)
# Tools: semantic_search, text_search, get_schema, system_status
RECALL_MCP_URL=http://localhost:3003
RECALL_MCP_ENABLED=true

# ailumina-bridge-mcp HTTP Server (Cross-Agent Communication)
# Tools: send-message, get-response, list-agents
BRIDGE_MCP_URL=http://localhost:3004
BRIDGE_MCP_ENABLED=true
BRIDGE_AILUMINA_URL=http://localhost:8000

# ===== OAuth 2.1 Configuration (Optional - for secure MCP access) =====
OAUTH_ENABLED=false
# OAUTH_CLIENT_ID=your_client_id_here
# OAUTH_CLIENT_SECRET=your_client_secret_here
# OAUTH_TOKEN_ENDPOINT=https://auth.example.com/oauth/token
# OAUTH_INTROSPECTION_ENDPOINT=https://auth.example.com/oauth/introspect
# OAUTH_ALLOWED_SCOPES=mcp:read,mcp:write

# ===== Development Tools =====
# These are automatically available when using docker-compose -f docker-compose.yml -f docker-compose.dev.yml up

# Neo4j Browser: http://localhost:7474
# Redis Insight: http://localhost:8001
# Redis Commander: http://localhost:8002
# Qdrant Dashboard: http://localhost:6333/dashboard
