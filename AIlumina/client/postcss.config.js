/**
 * postcss.config.js
 *
 * PostCSS configuration for the client application.
 * Note: Tailwind CSS 4.x is handled by @tailwindcss/vite plugin in vite.config.ts
 * This config is kept minimal for any additional PostCSS processing needs.
 */

export default {
  plugins: {
    // Minimal PostCSS config - Tailwind is handled by Vite plugin
  },
};
